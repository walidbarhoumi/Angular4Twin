export interface Product {
    id?: number;
    title?: string;
    description?: string;
    price?: number;
    image?: string;
    available?: boolean;
    nb_Likes?: number; 
    isPopular?: boolean; 
  }
  